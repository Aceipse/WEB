extends layout
block content
  - var books = ["A", "B", "C"]
  h1= "Fitness"
  input.btn-primary(
  type='button'
  name='agreement'
  )
  select
    each book, i in books
      option(value=i) Book #{book}
