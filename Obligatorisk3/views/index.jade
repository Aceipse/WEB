extends layout
block content
  - var books = ["A", "B", "C"]
  h1= "Fitness"
  input.btn-primary(
  type='button'
  value='Add User'
  )
  select
    each user, i in Users
      option(value=i) #{user.name}
